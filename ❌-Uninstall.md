## ❌ Uninstall OpenOMEN

**If you want to remove OpenOMEN from your system, all you have to do is delete it from your path:**

```bash
sudo rm /usr/local/bin/openomen
```
*If you don't know where your OpenOMEN executable is located, you can run `which openomen` to get its exact location.*